{
  "name": "annotations-anonymous",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=api & npm run dev --workspace=web"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.8"
  }
}
